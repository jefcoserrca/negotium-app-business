import { StoreHeaderDirective } from './store-header.directive';

describe('StoreHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new StoreHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
